
version: 2

models:
  - name: employee_count_per_department
    description: "Counts employees per department"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null